import React from 'react'

const JSXRules = () => {
  return (
    <div>
        <h1>JSXRules</h1>

        <p>
            <li>JSX must return a <em>single parent element</em></li>
            <li>JSX elements must be <em>properly closed</em></li>
            <li>JSX attributes are written using <em>camelCase</em> (e.g, <em>className</em> instead of <em>class</em>)</li>
        </p>
    </div>
  )
}

export default JSXRules